@* @model List<SafeBite_VNJP.Models.Customer>

@{
    ViewData["Title"] = "顧客一覧";
}

<h2>顧客一覧</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>会社名</th>
            <th>メール</th>
            <th>電話</th>
            <th>住所</th>
            <th>登録日</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.CustomerId</td>
                <td>@c.CustomerName</td>
                <td>@c.Email</td>
                <td>@c.Phone</td>
                <td>@c.Address</td>
                <td>@c.CreatedAt</td>
            </tr>
        }
    </tbody>
</table> *@